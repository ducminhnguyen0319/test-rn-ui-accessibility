import React from 'react';
import {Text} from 'react-native-paper';

const LocationPicker = () => {
  return <Text>Location Picker: reuse google location picker</Text>;
};

export default LocationPicker;
